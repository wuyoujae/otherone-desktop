import type { ModelConfig, ProviderConfig } from '../types/apiConfig';

export const createModelConfig = (index: number): ModelConfig => ({
  id: `model-${Date.now()}-${index}`,
  name: `custom-model-${index}`,
  contextLength: 8192,
  contextWindow: 8192,
  thresholdPercentage: 0.8,
  maxIterations: 8,
  temperature: 0.7,
  topP: 1,
  stream: true,
  parallelToolCalls: true,
  toolChoice: 'auto',
  extraParams: '',
  reasoningEffort: 'medium',
  defaultModel: false,
});

export const createProviderConfig = (index: number): ProviderConfig => ({
  id: `provider-${Date.now()}-${index}`,
  name: `自定义供应商 ${index}`,
  provider: 'OpenAI Compatible',
  officialUrl: '',
  baseUrl: 'https://api.example.com/v1',
  apiKey: '',
  models: [{ ...createModelConfig(1), name: 'new-model' }],
});

export const defaultApiConfigs: ProviderConfig[] = [
  {
    id: 'provider-openai',
    name: 'OpenAI',
    provider: 'OpenAI',
    officialUrl: 'https://platform.openai.com',
    baseUrl: 'https://api.openai.com/v1',
    apiKey: '',
    models: [
      {
        id: 'model-gpt-4o',
        name: 'gpt-4o',
        contextLength: 128000,
        contextWindow: 128000,
        thresholdPercentage: 0.8,
        maxIterations: 8,
        temperature: 0.7,
        topP: 1,
        stream: true,
        parallelToolCalls: true,
        toolChoice: 'auto',
        extraParams: '',
        reasoningEffort: 'medium',
        defaultModel: true,
      },
      {
        id: 'model-gpt-4o-mini',
        name: 'gpt-4o-mini',
        contextLength: 128000,
        contextWindow: 128000,
        thresholdPercentage: 0.75,
        maxIterations: 6,
        temperature: 0.4,
        topP: 0.9,
        stream: true,
        parallelToolCalls: true,
        toolChoice: 'auto',
        extraParams: '',
        reasoningEffort: 'low',
        defaultModel: false,
      },
    ],
  },
  {
    id: 'provider-deepseek',
    name: 'DeepSeek',
    provider: 'OpenAI Compatible',
    officialUrl: 'https://platform.deepseek.com',
    baseUrl: 'https://api.deepseek.com/v1',
    apiKey: '',
    models: [
      {
        id: 'model-deepseek-reasoner',
        name: 'deepseek-reasoner',
        contextLength: 64000,
        contextWindow: 64000,
        thresholdPercentage: 0.82,
        maxIterations: 10,
        temperature: 0,
        topP: 1,
        stream: true,
        parallelToolCalls: false,
        toolChoice: 'auto',
        extraParams: '',
        reasoningEffort: 'high',
        defaultModel: false,
      },
    ],
  },
];
